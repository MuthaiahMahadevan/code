You are given a string s. You need to reverse the string.

Example 1:

Input:
s = Geeks
Output: skeeG
Example 2:

Input:
s = for
Output: rof

class Reverse
{
    // Complete the function
    // str: input string
    public static String reverseWord(String str)
    {
        int n=str.length();
        for(int i=0;i<n;i++)
        {
                char t=str.charAt(i);
                str.charAt(i)=str.charAt(n-i-1);
                str.charAt(n-1-i)=t;
        }
        System.out.println(str);
        
    }
}
//
